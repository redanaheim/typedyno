/*import { Client, Message } from "discord.js";
import { PoolInstance as Pool } from "../../../pg_wrapper.js";

import { BotCommand, BotCommandProcessResults, BotCommandProcessResultType, Subcommand } from "../../../functions.js";
import { automatic_dispatch, command } from "../../../module_decorators.js";
import { DebugLogType, log, LogType } from "../../../utilities/log.js";
import { is_valid_Snowflake } from "../../../utilities/permissions.js";

@command()
export class TJList extends Subcommand<typeof TJList.manual> {
    constructor() {
        super(TJList.manual, TJList.no_use_no_see, TJList.permissions);
    }

    static readonly manual = {
        name: "list",
        arguments: [
            {
                name: "jump name",
                id: "jumprole_name",
                optional: false,
            },
        ],
        syntax: "<prefix>tj remove NAME $1",
        description: "Remove a Jumprole from your list.",
    } as const;

    static readonly no_use_no_see = false;
    static readonly permissions = undefined;
}
*/
